export interface Movement {
    dX: number,
    dY: number,
}

export interface Point {
    x: number,
    y: number,
}
export interface Boundaries {
    minX: number,
    maxX: number,
    minY: number,
    maxY: number,
}